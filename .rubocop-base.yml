AllCops:
  TargetRubyVersion: 3.2
  NewCops: disable # The existing codebase predates modern RuboCop; disabling ensures only our new changes are lint-checked if they fail.

Style/StringLiterals:
  EnforcedStyle: double_quotes

Style/FrozenStringLiteralComment:
  Enabled: false
